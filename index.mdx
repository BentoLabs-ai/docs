---
title: "Introduction"
description: "Production infrastructure for AI agents. Monitor what runs. Improve what fails. Compound learnings."
icon: "compass"
---

## Set up with an AI coding tool

Install the Bento docs skill so your AI coding tool has full context on the SDK. Copy this prompt into Claude Code, Cursor, Codex, OpenCode, or Windsurf:

~~~markdown
Help me integrate Bento into this project. Walk it step by step.

## 0. Load context

```bash
npx skills add https://docs.bentolabs.ai
```

Read `https://docs.bentolabs.ai/quickstart` and `https://docs.bentolabs.ai/python/integrations` before writing any code. The TypeScript SDK is not yet GA — if this is a Node project, point me at `https://docs.bentolabs.ai/typescript` and stop.

## 1. Discover

```bash
ls pyproject.toml setup.py requirements*.txt 2>/dev/null
grep -rlE "from fastapi|from flask|django\.|from starlette" --include="*.py" . | head -5

# Decides the path
grep -rnE "from google\.adk|google\.adk\." --include="*.py" .

# Other LLM SDKs (need bento.track_ai)
grep -rnE "openai\.|OpenAI\(|\.chat\.completions\.create" --include="*.py" .
grep -rnE "anthropic\.|Anthropic\(|\.messages\.create" --include="*.py" .
grep -rnE "bedrock-runtime|invoke_model|converse" --include="*.py" .
```

Summarize: framework, whether ADK is present, other LLM call sites, env file location. Confirm before editing.

## 2. Install

```bash
pip install bentolabs-sdk
# ADK in use? pip install "bentolabs-sdk[adk]"
```

Add `BENTOLABS_API_KEY=bl_pk_...` to the env file. Point me at https://platform.bentolabs.ai if I don't have a key.

## 3. Wire it up

If ADK is in use — three lines at startup:

```python
import bentolabs_sdk as bento

bento.init(
    user_id=lambda: get_current_user_id(),
    session_id=lambda: get_current_session_id(),
)
bento.instrument()
```

Bind the getters to whatever holds request-scoped identity (a ContextVar set by middleware, `request.state.user.id`, `g.user.id`).

For anything not covered, wrap each LLM call site with `bento.track_ai(event=, user_id=, convo_id=, model=, provider=, input=, output=)`. Bedrock model IDs (`anthropic.claude-3-...`) need `provider="aws_bedrock"`, not `"anthropic"`.

## 4. Verify

Run one real flow. Open https://platform.bentolabs.ai and confirm a row appears with model, user, session, input, and output populated. Empty columns → re-check Step 3 for that call site. For short scripts, call `bento.flush()` before exit.
~~~

For Claude Code, Codex, OpenCode, and Windsurf, see the [quickstart](/quickstart#other-ai-coding-tools) for per-tool install commands.

<Frame>
  <img src="/images/dashboard.png" alt="Bento traces view" />
</Frame>

## What you get

<CardGroup cols={2}>
  <Card title="Traces" icon="list-tree">
    Every span from every framework, captured via OpenTelemetry. Jump into the span tree or follow a signal straight to the call that broke.
  </Card>
  <Card title="Signals" icon="signal">
    Describe a failure mode in plain English. Bento trains a detector on your own traces, fires it in real time, and backfills your history.
  </Card>
  <Card title="Alerts" icon="bell">
    Write alerts in English. Bento groups fires into incidents and judges each one real or benign — so you only wake up for real drift.
  </Card>
  <Card title="Evaluations & versions" icon="rotate">
    Close the loop. Promote good behavior, regression-test new versions on captured traces, ship the next version with confidence.
  </Card>
</CardGroup>

## Start in three minutes

<CardGroup cols={2}>
  <Card title="Quickstart" icon="bolt" href="/quickstart">
    Install the SDK, send your first trace.
  </Card>
  <Card title="Integrations" icon="plug" href="/python/integrations">
    One line to trace Google ADK.
  </Card>
</CardGroup>

## Pick your SDK

<CardGroup cols={2}>
  <Card title="Python" icon="python" href="/python/installation">
    `pip install bentolabs-sdk`. Google ADK integration, plus manual tracking for everything else.
  </Card>
  <Card title="TypeScript" icon="js" href="/typescript">
    In active development. Node and browser support coming soon.
  </Card>
</CardGroup>
