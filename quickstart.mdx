---
title: "Quickstart"
description: "Install Bento, send your first trace, see it in the dashboard."
icon: "bolt"
---

import InstallSdk from "/snippets/install-sdk.mdx";
import ApiKeySetup from "/snippets/api-key-setup.mdx";

<Steps>
  <Step title="Get an API key">
    <ApiKeySetup />
  </Step>

  <Step title="Install">
    ```bash
    pip install bentolabs-sdk
    ```

    Using Google ADK? Install the extra so Bento can capture every call automatically:

    ```bash
    pip install "bentolabs-sdk[adk]"
    ```
  </Step>

  <Step title="Send your first trace">
    Pick the one line of code that matches your stack.

    **Google ADK** — capture everything in one line:

    ```python
    import bentolabs_sdk as bento

    bento.init()
    bento.instrument()

    # Run your existing ADK agent. Bento captures it.
    bento.flush()
    ```

    **Anything else** (OpenAI, Anthropic, Bedrock, Vertex, …) — wrap each LLM call site:

    ```python
    import bentolabs_sdk.analytics as bento

    bento.track_ai(
        event="user_message",
        user_id="user_42",
        convo_id="conv_abc",
        model="gpt-4o",
        provider="openai",
        input="What's the capital of France?",
        output="Paris.",
    )
    bento.flush()
    ```

    Open [platform.bentolabs.ai](https://platform.bentolabs.ai) — the trace lands within seconds.
  </Step>
</Steps>

## Set up with an AI coding tool

<Prompt description="Install the Bento docs skill so your AI coding tool has full context on the SDK." actions={["copy", "cursor"]}>
  Help me integrate Bento into this project. Walk it step by step.

  ## 0. Load context

  ```bash
  npx skills add https://docs.bentolabs.ai
  ```

  Read `https://docs.bentolabs.ai/quickstart` and `https://docs.bentolabs.ai/python/integrations` before writing any code. The TypeScript SDK is not yet GA — if this is a Node project, point me at `https://docs.bentolabs.ai/typescript` and stop.

  ## 1. Discover

  ```bash
  ls pyproject.toml setup.py requirements*.txt 2>/dev/null
  grep -rlE "from fastapi|from flask|django\.|from starlette" --include="*.py" . | head -5

  # Decides the path
  grep -rnE "from google\.adk|google\.adk\." --include="*.py" .

  # Other LLM SDKs (need bento.track_ai)
  grep -rnE "openai\.|OpenAI\(|\.chat\.completions\.create" --include="*.py" .
  grep -rnE "anthropic\.|Anthropic\(|\.messages\.create" --include="*.py" .
  grep -rnE "bedrock-runtime|invoke_model|converse" --include="*.py" .
  ```

  Summarize: framework, whether ADK is present, other LLM call sites, env file location. Confirm before editing.

  ## 2. Install

  ```bash
  pip install bentolabs-sdk
  # ADK in use? pip install "bentolabs-sdk[adk]"
  ```

  Add `BENTOLABS_API_KEY=bl_pk_...` to the env file. Point me at https://platform.bentolabs.ai if I don't have a key.

  ## 3. Wire it up

  If ADK is in use — three lines at startup (FastAPI lifespan, Django `apps.py:ready()`, or the worker entry-point):

  ```python
  import bentolabs_sdk as bento

  bento.init(
      user_id=lambda: get_current_user_id(),
      session_id=lambda: get_current_session_id(),
  )
  bento.instrument()
  ```

  Bind the getters to whatever holds request-scoped identity (a ContextVar set by middleware, `request.state.user.id`, `g.user.id`).

  For anything else, wrap each LLM call site with `bento.track_ai(event=, user_id=, convo_id=, model=, provider=, input=, output=)`. Bedrock model IDs (`anthropic.claude-3-...`) need `provider="aws_bedrock"`, not `"anthropic"`.

  ## 4. Verify

  Run one real flow. Open https://platform.bentolabs.ai and confirm a row appears with model, user, session, input, and output populated. Empty columns → re-check Step 3 for that call site. For short scripts, call `bento.flush()` before exit.
</Prompt>

### Other AI coding tools

<Tabs>
  <Tab title="Claude Code">
    ```bash
    npx skills add https://docs.bentolabs.ai
    claude mcp add --transport http bentolabs-docs https://docs.bentolabs.ai/mcp
    ```

    Run `claude` in your project and paste the prompt above.
  </Tab>

  <Tab title="Codex">
    ```bash
    npx skills add https://docs.bentolabs.ai
    ```

    Add to `~/.codex/config.toml`:

    ```toml
    [mcp_servers.bentolabs-docs]
    type = "http"
    url = "https://docs.bentolabs.ai/mcp"
    ```

    Start `codex` and paste the prompt above.
  </Tab>

  <Tab title="OpenCode">
    ```bash
    npx skills add https://docs.bentolabs.ai
    ```

    Add to `opencode.json`:

    ```json
    {
      "mcp": {
        "bentolabs-docs": {
          "type": "remote",
          "url": "https://docs.bentolabs.ai/mcp"
        }
      }
    }
    ```

    Run `opencode` and paste the prompt above.
  </Tab>

  <Tab title="Windsurf">
    ```bash
    npx skills add https://docs.bentolabs.ai
    ```

    In Cascade, add a custom MCP server with URL `https://docs.bentolabs.ai/mcp`, then paste the prompt above.
  </Tab>
</Tabs>

## Next

<CardGroup cols={2}>
  <Card title="Integrations" icon="plug" href="/python/integrations">
    Capture Google ADK with one line.
  </Card>
  <Card title="Manual tracking" icon="paper-plane" href="/python/track-ai">
    Wrap individual LLM calls with `bento.track_ai`.
  </Card>
  <Card title="Configuration" icon="gear" href="/python/configuration">
    Env vars, identity getters, lifecycle.
  </Card>
  <Card title="From Langfuse" icon="/images/langfuse.svg" href="/migrations/langfuse">
    Mechanical translation guide.
  </Card>
  <Card title="From Raindrop" icon="/images/raindrop.svg" href="/migrations/raindrop">
    Mechanical translation guide.
  </Card>
</CardGroup>
